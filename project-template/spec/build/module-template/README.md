# <模块名>（<module-id>）

<一句话定位：这个模块管什么。>

- **适用**：<什么项目 / 什么情况启用>
- **不管**：<明确不负责什么>

- **启用**：agent 按 `MODULE.md` 的规范（含 `assets/` 里的模板）在 <落点>
  例化 <产出物>，之后负责维护。
- **停用**：<停用时什么停止、什么保留；默认只停维护、不删产物>。

给 agent 的规范见 `MODULE.md`，结构化声明见 `module.json`。
