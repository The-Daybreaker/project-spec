# CHANGELOG — {{PROJECT_NAME}} 变更历史

> 每次发布必更新：在顶部新增条目，版本与 `VERSION` 文件一致。
> 格式：`## vX.Y.Z（YYYY-MM-DD）` + 变更要点（feat / fix / docs / refactor / chore）。

## v0.1.0（{{DATE}}）

- 初始版本：由通用项目模板初始化。
  - 建立三区文件归属（公开 / private / 不管理），private 子 git 内部管理。
  - 建立 Agent+人协作开发工作流（先对齐后实施、实施后自动审计、验证后发布）。
  - 建立版本管理与 CI/CD（VERSION + git tag + GitHub Actions 自动发布）。
