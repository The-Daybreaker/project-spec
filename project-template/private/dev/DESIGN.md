# DESIGN — {{PROJECT_NAME}} 设计文档（当前状态）

> 模块：混合（【通用】= 归属规则/工作流指针/开发规范/测试；【项目专用】= 项目形态/设计/打包/环境）。
> 只描述**当前版本（v{{VERSION}}）**的设计、工作流与开发规范；历史演进见
> `CHANGELOG.md`（同目录）。冲突时以本文档与根 `AGENTS.md` 为准；个人/机器专属
> 细节以 `../AGENTS.md` 为准。

## 【项目专用】项目形态

（按项目实际填写：技术栈、模块划分、依赖方向、对外接口/契约。）

- 技术栈：<填写>
- 模块：<填写>
- 依赖方向与契约：<填写>

## 【通用】文件分类归属（三区矩阵，开发强制要求）

（与根 AGENTS.md 一致；本文件保留开发视角的细则。见根 `AGENTS.md`「仓库布局与
文件分类归属」与 `../AGENTS.md`「仓库布局与文件分类归属」。）

## 【项目专用】核心设计（当前版本）

（按项目实际填写：架构、关键流程、数据模型、配置 schema、自动化设计等。）

### 【通用】每次对话前置（bootstrap）

任何任务开始前：读根 `AGENTS.md` → 读 `private/AGENTS.md` → 查看两个 git 状态 →
读 `version.json` 与 `CHANGELOG.md` 顶部 → 读 `WORKLOG.md`（恢复进行中进度）→ 读
`TEST-REPORT.md`。**上下文压缩后或新对话开始时必须完成以上重读（红线 15）**；
WORKLOG 内容膨胀或旧任务已完结时，先询问用户是否清理（归档/移入 `_trash/`）。
保证任何新对话、任何 agent 都能从零接手，不依赖记忆。

### 【通用】约束收口

- agent 读的个性化约束统一存 `private/AGENTS.md`（本机环境、用户决策）；
- 通用红线在根 `AGENTS.md`；审计清单在 `../../docs/audit-checklist.md`。

## 【通用】开发工作流（强制，每次需求都走完）

完整工作流见根 `AGENTS.md`「开发工作流」与 `../AGENTS.md`「开发工作流」（权威源，
本文件不重复），本文件只保留设计相关细则。

## 【通用】开发规范

1. **版本号**：版本号**从 `0.0.1` 开始**；每次默认只升最后一位（patch）；
   **前两位（major/minor）增加必须向用户确认**；破坏性变更必须用户确认并升主版本、
   说明迁移方案。
2. **提交信息**：普通提交 `feat:/fix:/docs:/chore:/refactor: - 描述`（不带版本号）；
   **发布提交**带版本号 `feat: vX.Y.Z - 描述`。
3. **发布策略**：以 `../AGENTS.md`「项目状态与版本」的发布策略为准（默认不自动
   发布；初始化时可用 `--auto-release` 开启自动发布）。
4. **文档及时更新**：开发期文档（CHANGELOG / DESIGN / TEST-REPORT / WORKLOG /
   EXPERIENCE-TO-TEMPLATE / EXPERIENCE-TO-KB）在改动完成时立即同步，不滞后
   （「改动完成即文档就绪」）。
5. **发布前必测**：检查命令 + 项目测试 + TEST-REPORT 记录（由 agent 执行）。
6. **发布前必审计**：自审 + 独立 agent 审计，发现修复后才允许发布。
7. **private 纪律**：个人/开发期文件一律进 `private/`；发布前必须先同步 private
   子 git。
8. **公开文档只写最终结果**：讨论过程、舍弃方案等开发期信息不写入公开文档；
   设计决策只记录于 `private/`。
9. **经验沉淀**：每轮对话结束时，将本轮**完整候选经验**写入
   `EXPERIENCE-TO-TEMPLATE.md`（可复用进模板）与 `EXPERIENCE-TO-KB.md`（可进知识库），
   沉淀时以这两份文档为唯一依据；每次项目架构发生改变（无论是否发布）以及项目
   每次更新（发布）之后，提醒用户真正沉淀（模板经验集成进通用项目模板；知识库
   经验按 knowops 等规范沉淀）；沉淀与否、沉淀到哪里由用户决定，agent 负责记录
   候选与提醒。
10. **立项调研先行**：讨论项目思路/需求/架构/功能/产品等**立项类话题**时，优先
    在 GitHub 调研现成参考（相似项目、方案、库），向用户展示调研结果并提醒
    **「先调研再立项」**；未经调研不引导用户立项（根 `AGENTS.md` 红线 13）。
11. **阶段落盘**：每完成一小阶段先更新 `WORKLOG.md` 与受影响文档再继续
    （根 `AGENTS.md` 红线 14）。
12. **上下文恢复重读**：上下文压缩后或新对话开始时，必须重读红线规范与相关文档
    再继续（根 `AGENTS.md` 红线 15）。
13. **模板升级**：模板发布新版本时，按 `../../docs/UPGRADE.md` 只应用【通用】模块
    变更，并更新根 `version.json` 的 `template_version`。
14. **删除纪律**：对话内删除先移入 `_trash/<agent>_<日期>_<时分>/`，任务结束时
    用 `scripts/trash.py` 整体进回收站（根 `AGENTS.md` 红线 4）。

## 【项目专用】关键不变量（改动后必须仍成立）

（按项目填写：任何改动都不得破坏的约束，如幂等 / 不丢状态 / 可迁移性 / 单一真相等；
改动前先核对本清单，改动后必须验证。）

- <不变量 1>
- <不变量 2>

## 【项目专用】改动影响面定位表（你要动的 → 影响范围 → 必须同步检查）

| 你要动的 | 影响范围 | 必须同步检查 |
|---|---|---|
| <模块/文件> | <...> | <...> |

## 【通用】测试（发布前必跑）

1. **检查命令**：`scripts/ci_check.py`（lint / build / test，按项目实现）。
2. **真实场景测试**（如有）：在 `../test/` 按场景清单实际执行并核对；记录到
   `TEST-REPORT.md`。
3. **明文例外（仅此一种）**：本次改动不涉及运行时文件、且用户明确确认时，测试
   结论可沿用上一版本，必须在 TEST-REPORT 注明「用户确认沿用」；禁止 agent 自行
   省略或沿用。

## 【项目专用】打包与发布

（按项目实际填写：产物清单、打包命令、发布渠道、安装/部署步骤。）

## 【项目专用】环境与已知坑

（本机环境细节见 `../AGENTS.md`「本机环境」，本文件不重复机器专属信息；只保留
与设计相关的坑。）

- <填写>
