# CHANGELOG — 构建规则（build）

本文件记录构建规则层的版本变更，方便跨版本迁移。版本号遵循 SemVer，
变更分类参考 Keep a Changelog（Added / Changed / Removed / Fixed）。

## [0.1.0] - 2026-09-04
### Added
- 建立构建规则层的版本身份：新增 `build.json`（id / name / version /
  description）与本 CHANGELOG，云端 `registry.json` 登记 build 条目。
- 既有内容：`build.md`（构建规则）+ `spec-template/`（spec 模板）+
  `module-template/`（模块模板）。
