# <模块名>（<module-id>）

<定位段，2-3 句：这个模块管什么、产物落在项目哪个位置、什么形态
（每期一份 / 活文档 / 按需生成）。>

- **适用**：<什么项目 / 什么情况启用；与 MODULE.md「四、适用与边界」一致>
- **不管**：<明确不负责什么；与 MODULE.md「四、适用与边界」一致>

- **启用**：<按模块性质二选一：
  - 模板实例化型：agent 按 `assets/<模板>`（仅参考格式）在 <落点>
    实例化 <产出物>，之后负责维护。
  - 动作执行型：agent <核心动作>，之后负责维护。>

给 agent 的规范见 `MODULE.md`，结构化声明见 `module.json`。
