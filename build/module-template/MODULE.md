# MODULE.md — <模块名>（模块规范）

> **适用范围**：当 agent 负责维护本模块的产出——<列出产出物，如
> context/xxx.md> 时，加载并遵循本文件。项目级总纲见仓库根 `AGENTS.md`。
>
> 本文件是「<模块名>」模块自带的 agent 规范，随模块例化。

## 一、产物目录与例化

本模块的产出落在哪些目录 / 文件，以及启用时用 `assets/` 里哪个模板例化：

```
<目录树或文件列表，如：
context/
  xxx.md>
```

- <产物 A>（包含什么）← 用 `assets/<模板A>` 例化
- <产物 B>（包含什么）← 用 `assets/<模板B>` 例化

## 二、运行过程

agent 承担本模块任务时，按什么步骤走（写到新 agent 照着能一步步做）：

1. <步骤一>
2. <步骤二>

## 三、规范

本模块特有的规则：

- <规则一>
- <规则二>

## 四、适用与边界

- **适用**：<什么项目 / 什么情况启用>
- **不管**：<明确不负责什么>
