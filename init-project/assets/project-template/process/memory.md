# memory.md — agent 记忆

> agent 自我维护的记忆区：对话上下文是临时的、会丢失，这里持久保存。
> 冷启动（开始·读盘）必读。本文件**只声明用途、不声明内容**——放什么、怎么组织
> 由 agent 自行判断（一条一段，写清情境与结论；宁精勿滥）。只存**当前
> 有效**的条目；失效 / 过时的整段移出，归档 `archive/memory/`（日期前缀）。
> 记进来就代表下次遇到同样情境必须先看这里。
