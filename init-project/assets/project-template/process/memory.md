# memory.md — agent 记忆

> agent 在开发过程中踩过的坑、被验证过的判断，写在这里留给后续会话——
> 对话上下文是易失内存，这里是磁盘。冷启动（开始·读盘）必读。
> 只放**当前有效**的条目；失效 / 过时的整段移出，归档 `logs/memory/`
> （日期前缀）。一条一段：什么情境 + 什么坑 / 什么判断；宁精勿滥，
> 记进来就代表下次遇到同样情境必须先看这里。

## 踩过的坑

## 被验证过的判断
