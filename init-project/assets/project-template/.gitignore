# 宪章红线落地：删除先进 _trash/（临时删除区，不入版本库）
_trash/

# 宪章红线落地：密钥不入库（常见密钥文件名模式，机械防线）
# 宁可误伤不可漏收——误伤的文件可显式 git add -f 找回；
# 项目特定的密钥命名由项目自行追加
.env
*.env
*.key
*.pem
secrets/
credentials*
*_secret*

# 密钥扫描脚本自身不是密钥（名字含 secret 被上一条 *_secret* 误伤），显式放行
!scripts/scan_secrets.py

# 排除名单会写本机用户名等个人信息，绝不入库
scripts/scan_secrets.ignore
