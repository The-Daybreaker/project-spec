# CHANGELOG

发布仓库的版本历史（三产物：project-template / init-project / agent-rules）。
模板本体的变更记录另见 `project-template/CHANGELOG.md`，其版本号与发布版本
解耦、各自演进。

v1 各版本的完整明细原存于私有开发文档（未随仓库公开），此处为摘要；细节
可按 git tag（`v1.0.1` ~ `v1.6.1.patch1`）追溯对应提交。

## [1.7.0] - 2026-09-02

断代版本：模板整体重设计为 v3，目录结构全新。v1 项目无法原地升级，
老项目继续按 v1 运转即可，新项目用本版。

### Added

- 七件套骨架：`AGENTS.md`（协作总纲 + 宪章 6 条）、`README.md`（使用手册）、
  `context/`（项目上下文，种类由 spec 决定）、`workspace/`（source +
  delivery）、`process/`（任务板 + inbox / pending / reviews 三件套）、
  `logs/`（历史归档）、`spec/`（声明式规范）。
- spec 机制：构建规则（`spec/build/`：build.md + spec / module 两模板）+
  预置 spec `software-dev`（vision / design / prd / adr / development /
  test / audit / release 共 8 模块）；空 spec 时项目以地基形态运转。
- 推送前密钥门禁（保留自 v1 并参数化）：`scripts/scan_secrets.py` +
  `.githooks/pre-push`，排除名单外置 `scan_secrets.ignore`（不入库）。
- 发布仓库顶层 `CHANGELOG.md`（本文件，含 v1 各版本摘要）。

### Changed

- `init-project` skill 重构：内嵌 v3 模板镜像，初始化含 hooksPath 配置与
  回读校验清单（内联 SKILL.md）。
- `agent-rules` skill 重构：模板宪章的项目外精简形态（version 1.7.0）。
- 发布 README 重写（价值主张 / 快速开始 / 目录结构 / 版本说明）。

### Removed

- v1 结构整体移除：`private/` 子仓库、`docs/`、`dist/`、`archive/`、
  `.github/workflows/`、`version.json`、`install-targets.json` 及大部分
  维护脚本。

## [1.6.1.patch1] - 2026-08-28

红线 18 补齐显式三要素（意图 / 展示面 / 验收面，19 条红线三要素终检
齐全）；文档质量可持续机制——文档质量标准（四条标准 + 反 AI 感清单 +
好坏对照例）与审计文档质量关落地。

## [1.6.1.patch0] - 2026-08-28

接口设计与设计环节补全：设计契约文件夹（架构 / 接口 / 数据 / 安全，适用
必做、不适用须声明）；接口契约场景化——用户审场景映射表、不审技术，确认
后冻结，验收用例联动；阶段契约与审计清单联动补项。

## [1.6.0.patch0] - 2026-08-28

漂移免疫与文档架构改造（断代版本，不向前兼容）：单一真相源三层结构——
15 条规范事实各有唯一家（事实台账 + FACT/REF/INJECT 锚点机制）；漂移免疫
门禁（重复检测 / 摘要限长 / 正文指纹校验）；文档集归一（FLOW /
USER-GUIDE 废除并入他处）；阶段契约（每阶段必做清单 + 裁剪属性）；需求
冲突检测入口闸；测试台账；安装目标扩至九处。

## [1.5.0.patch0] - 2026-08-27

红线与要求可验证性改造：新增最顶层红线「要求三要素元规范」（所有红线
须同时具备意图 / 展示面 / 验收面，自指示范）；新增红线「敏感信息与私有区
边界」（密钥内容级扫描 + 推送前安全门禁三层）；「范围克制」与「提问共识」
两条红线重写（番茄炒蛋比喻入正文、共识卡机制）；测试正式子阶段与测试
报告双区；辩护性措辞扫描；历史泄露处置与全历史身份重写。

## [1.4.2.patch0] - 2026-08-27

第九轮全面审计修复收口：新项目首跑即红等 5 项发现全修 + 表述面同类残留
清理；防再发机制——开箱即用冒烟自检（初始化 + 回读 + 骨架脚本自检，
发版前必绿）。

## [1.4.1.patch0] - 2026-08-27

新增红线「agent 提问与共识确认机制」（禁问题面板 / 回答≠确认 / 共识
快照 + 逐项表态）；阶段卡、生命周期合规清单、反定型合并为紧凑模块；版本
号体系改为四段式 `X.Y.Z.patchN`。

## [1.4.0] - 2026-08-26

整体架构重构（模块化改造）：16 节点流程收敛为 P1 需求 / P2 方案 / P3 开发 /
P4 审计验证 / P5 交付发布五模块；WORKLOG → STATUS 状态快照化；阶段卡 =
合规检查点；文档披露渐进式（流程 / 用户指南 / 加载规则 / 阶段定义四件）；
需求引导方法论（禁抛选项）。

## [1.3.2] - 2026-08-26

全生命周期专业审计修复：三域并行独立子代理审计（规范与文档语言 / 脚本与
机制 / 流程闭环与生命周期），处置一致性缺陷、描述准确性、脚本健壮性共
16 项（含照抄模板即失败类问题的修复）。

## [1.3.1] - 2026-08-26

`src/` 代码区实体化与分区约定：业务源码统一入 `src/`，根目录为工程区。

## [1.3.0] - 2026-08-26

新增宪章「范围克制与纠错清零」（不做需求外的添加、不为未做之事补写
说明）；图可视化规范——流程图 / 架构图 / 页面原型先出图确认再执行；
两 skill 合并入 `skills/` 目录。

## [1.2.2] - 2026-08-26

第七轮全面审计修复：安装面机制化——安装表（两 skill × 各 agent 目录的
单一事实来源）+ 安装副本全量哈希校验；CHANGELOG 顶部改 agent 手工更新；
发版前检查占位符双向断言等脚本加固。

## [1.2.1] - 2026-08-26

维护收口：升级路径补齐（私有骨架迁移规则 + 按版本迁移检查表）；流程提示
（缩写附中文翻译、流程位置展示）；初始化流程增强。

## [1.2.0] - 2026-08-26

「开发前阶段」规范大版本：PRD / RFC / ADR / RESEARCH 四登记册体系（状态
机 + 编号规则 + 索引）；文档治理扩展（历史文档区唯一允许正文留史）；状态
机三层约束（流程层 / 检查层 / 工具层校验脚本）；16 节点流程清单与流程
提示。

## [1.1.2] - 2026-08-26

目录结构重组（README 回根、文档入 `docs/`）；版本机制合并为根
`version.json`；项目归档 / 退役流程；`dist/` 发布产物约定；新增
`agent-rules` skill（模板通用规范派生的精简版全局规范）并首次安装到各
agent。

## [1.1.1] - 2026-08-25

WORKLOG 模板增强（「下次从这里继续」「人工介入点」）；定案清单与「必须
询问人类」清单；审计清单变更分级与质量门禁；文档地图。

## [1.1.0] - 2026-08-25

脚本由 PowerShell 迁移为 Python（版本递增 / CI 检查 / 发版前检查 / 模板
同步 / 回收站删除）；新增模板版本号与升级指南；文档双模块约定（【通用】/
【项目专用】，升级只覆盖通用模块）；红线扩至 15 条（阶段落盘、上下文恢复
重读）。

## [1.0.2] - 2026-08-15

立项调研先行：讨论项目思路 / 需求 / 架构时优先调研现成参考并提醒用户
「先调研再立项」。

## [1.0.1] - 2026-08-15

版本规范（0.0.1 起步，前两位递增须用户确认）+ 经验沉淀流程（知识库与
模板双通道）。

## [1.0.0] - 2026-08-15

首个版本：通用项目模板 + init-project skill。
