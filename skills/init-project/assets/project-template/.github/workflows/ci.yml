# CI — 持续集成检查（本地与 CI 共用 scripts/ci_check.py 入口）
# 按项目技术栈在此追加工具链与检查步骤（见下方注释示例）。

name: CI

on:
  push:
    branches:
      - "{{DEFAULT_BRANCH}}"
  pull_request:
    branches:
      - "{{DEFAULT_BRANCH}}"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run project checks
        run: python scripts/ci_check.py

      # --- 按项目技术栈追加（示例） ---
      # Node 项目：
      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 22
      #     cache: 'npm'
      # - name: Install dependencies
      #   run: npm ci
      # - name: Build
      #   run: npm run build
      # - name: Test
      #   run: npm test
      #
      # Python 项目：
      # - name: Install dependencies
      #   run: pip install -r requirements-dev.txt
      # - name: Test
      #   run: python -m pytest
      #
      # Rust 项目：
      # - name: Setup Rust
      #   uses: dtolnay/rust-toolchain@stable
      # - name: Check
      #   run: cargo check --all-targets
      # - name: Test
      #   run: cargo test
