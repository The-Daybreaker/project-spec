# STATUS — 通用项目模板工作区 当前状态快照

> 模块：项目专用。
> 只存**最新状态**；历史由 git 承担（`git log` 本文件）。阶段完成/收尾时**覆盖**更新，不追加。
> 新对话/压缩后按红线 15 重读：根 `AGENTS.md` → 模板 `AGENTS.md` → `private/AGENTS.md` → 本文件 → 「任务影响清单 → 要读文档清单」。

- 最后更新：2026-08-27 00:44

## 当前任务

- 需求：重设计「阶段卡 + 生命周期合规清单 + 反定型」为合并紧凑模块（已完成，P1-P4 收口）
  ——①标题含状态（括号内不保留其他内容）；②横置阶段线（标签=阶段中文名、当前节点
  加粗、只显示当前阶段节点）；③合规两行（✓ 已完成 / ⏳ 待完成，分号分隔）；④反定型
  6 项含内容紧凑排版（非仅勾选）；⑤合并为一整个模块；⑥展示只用中文名称、不显示
  字母缩写（保留数字编号）；⑦去掉「正在完成/已完成/下一步」字段。
- 目标/验收：模板【通用】PHASES §5 定义 + STATUS 骨架 + AGENTS 展示规则 + 审计清单
  + agent-rules 精简版同步 + 资产镜像 + 六处副本重装 + 全链验证绿。

## 当前阶段

- 模块：审计验证阶段（节点 12-13）｜ 子阶段：审计 + 验证收口｜ 状态：✅ 已完成

## 📇 阶段卡（最新）

## 📍 阶段卡（✅ 已完成）

审计验证阶段：12 自动审计 → **13 验证**

合规：
✓（已完成）：文档同步；状态落盘；阶段提交；质量校验；阶段确认；经验沉淀
⏳（待完成）：无

反定型：
- 当前理解：阶段卡/合规/反定型合并为紧凑模块，横线标注当前位置，全中文展示（已实施）
- 我的假设：横线只显示当前阶段节点；保留数字编号；反定型含完整内容（已实施）
- 尚未确认：无（设计经注释修订 2 轮 + 用户确认）
- 反例：中文节点名过长时横线会换行（接受：节点名已精简）
- 替代解释：核心是「一眼看清位置与差距」+ 降低缩写理解成本（已实施）
- 重新审视：字段已按用户意见收敛（无正在完成/已完成/下一步）

## 任务影响清单

- 影响文件：模板 `project-template/{AGENTS.md, private/AGENTS.md,
  private/dev/{PHASES,STATUS}.md, docs/audit-checklist.md}`、
  `skills/agent-rules/SKILL.md`、`skills/init-project/assets/project-template/`（镜像）、
  工作区 `AGENTS.md`、`docs/{STATUS,CHANGELOG,EXPERIENCE-TO-KB}.md`、六处安装副本（重装）
- 依赖文档：共识确认（阶段卡合并模块设计：注释修订 2 轮 + 用户确认）
- 要读文档清单（恢复时逐份读）：根 `AGENTS.md` → 模板 `project-template/AGENTS.md` →
  模板 `project-template/private/AGENTS.md` → 本文件 → 模板
  `project-template/private/dev/PHASES.md`

## 下一阶段输入预告

- 下一阶段：交付发布（如需发版 `v1.4.1.patch0`，另行确认）
- 输入：未发版变更区段（红线 17 提问与共识确认 + 阶段卡合并模块条目）
- 预期产物：按发版流程（bump `--part` / CHANGELOG 顶部 / pre_release_check /
  tag `v1.4.1.patch0` + Release）
