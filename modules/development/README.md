# 开发执行（development）

读需求定目标，拆成任务记到框架任务板，然后照着开发——代码 / 产物落在
`workspace/source/`，按任务逐步产出。agent 全权执行，小问题记下做完反馈、
大问题停下来回讨论。

- **适用**：写代码 / 产物的开发。
- **不管**：审查、发布。

- **启用**：agent 读相关文档定目标、拆任务，照着开发，之后负责维护。

给 agent 的规范见 `MODULE.md`，结构化声明见 `module.json`。
