# 开发执行（development）

读当期需求定目标，拆成任务记到框架任务板，然后照着开发——agent 全权执行，
小问题记下做完反馈、大问题停下来回讨论。

- **适用**：写代码 / 产物的开发。

- **不管**：做什么、怎么建。

- **启用**：agent 读依赖文档定目标、拆任务，照着开发，之后负责维护。

- **停用**：只停维护，产物留在原处。

给 agent 的规范见 `MODULE.md`，结构化声明见 `module.json`。
