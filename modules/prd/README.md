# 当期需求（prd）

这一期做什么——每期一份 PRD，记在 `context/prd/` 目录里，编号递增。
PRD 是当期活文档：开发中随时改，交付即定格。

- **适用**：按版本 / 期开发的项目。
- **不管**：长期方向、怎么实现。

- **启用**：agent 按 `assets/prd-template.md`（仅参考格式）在 `context/prd/`
  下例化当期 PRD，之后负责维护。

给 agent 的规范见 `MODULE.md`，结构化声明见 `module.json`。
