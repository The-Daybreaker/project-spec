# 测试与验收（test）

证明这期真做完了——按用例测、用户体验兜底。产物落在 `context/test/`，
每期一份测试文档、编号递增。小需求小修复免全套。

- **适用**：需要验证的产出。
- **不管**：审查、发布。

- **启用**：agent 按用例测，测不了的交用户兜底，之后负责维护。

给 agent 的规范见 `MODULE.md`，结构化声明见 `module.json`。
