# 收口（release）

一期完成的收口动作——模板只留位子，具体怎么收口每个项目自己定：私有打 tag、
本地安装即发布，公开上传 GitHub。交付物落 `workspace/delivery/`，给用户的
版本日志写 `workspace/CHANGELOG.md`。不可逆，动手前征得同意。

- **适用**：一期开发完成需要收口的项目。
- **不管**：开发、测试、审查、不更新用户版本号的小修改。

- **启用**：agent 确认收口方式，征得同意后执行，之后负责维护。

给 agent 的规范见 `MODULE.md`，结构化声明见 `module.json`。
