interface:
  display_name: "Agent Universal Rules (Lite)"
  short_description: "Global agent behavior rules (derived from the universal project template); loaded for any non-chat task."
  default_prompt: "Follow the agent-rules skill (general behavior rules) for this task."
