# 本工作区（模板仓库）自身的行尾归一化（与模板 project-template/.gitattributes 一致）
# 文本文件统一 LF（跨平台一致，含 Windows 脚本；与 .editorconfig end_of_line = lf 一致）。
* text=auto

*.py text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
*.toml text eol=lf
*.ps1 text eol=lf
*.bat text eol=lf
