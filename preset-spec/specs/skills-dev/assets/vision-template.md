# vision — skill 想法池

> 本模板仅为参考格式，具体内容由项目自行实现和改动。
> 由「愿景（想法池）」阶段实例化时按此骨架生成 `context/vision.md`。
> skill 体量小、一个 idea 一次就能开发完，故只记想法池、无排期层。
> vision 是活文档，随时可写、不设状态标记。

## 想法池

（每个想法一条：想做什么 skill、给谁用、解决什么；未定的标「未定」。
新想法先进这里，要做时从池里挑一个进入 design / development。）

## 已做（可选）

（已经开发完的 skill 一句话记一行，便于回看；不留过程历史，历史交给 git。）
